{
  "id": "50",
  "title": "Drop Columns",
  "difficulty": "Easy",
  "category": "Transformations",
  "description": "Remove specified columns from DataFrame.\n\nYou have a DataFrame with columns `keep1`, `keep2`, `col1`, `col2`, `col3`. Drop columns `col1`, `col2`, and `col3`.",
  "starter_code": "def etl(input_df):\n    # Write code here\n    pass",
  "test_setup": "from pyspark.sql import SparkSession\nfrom pyspark.sql import functions as F\nfrom pyspark.sql import Window as W\n\nspark = SparkSession.builder.appName('test').getOrCreate()\n\ndata = [('A', 1, 'x', 'y', 'z'), ('B', 2, 'a', 'b', 'c')]\ninput_df = spark.createDataFrame(data, ['keep1', 'keep2', 'col1', 'col2', 'col3'])",
  "test_validation": "result = etl(input_df)\nassert result is not None, 'Function returned None'\nassert len(result.columns) == 2, f'Expected 2 columns, got {len(result.columns)}'\nassert 'keep1' in result.columns and 'keep2' in result.columns, 'Missing kept columns'\nprint('✅ TEST PASSED!')",
  "hints": [
    "Pass multiple column names to drop",
    "Can also pass Column objects"
  ],
  "solution": "def etl(input_df):\n    return input_df.drop('col1', 'col2', 'col3')"
}
